\# TASK-2026-08-03-903f355-PROTOCOL-CONTENT-REGISTERS-AND-RENDERING

\#\# Цель

Исправить содержательную генерацию шаблонов \`project\_detailed\` и \`management\_summary\`, не ломая рабочую цепочку TXT/MD → LLM → HTML → Confluence → Telegram.

Репозиторий: \`EVErmeev/bitlink-bot\`.  
Ветка: \`fix/audit-e7cc95f\`.  
Проверенный head: \`903f355365514dd198c95fafd4f5f265100975e6\`.  
PR оставить Draft. Merge не выполнять. \`accepted\` не устанавливать.

\#\# Контрольный контекст

Использовать ту же расшифровку от 31.07.2026:

\- клиент: Роял Фуд;  
\- проект: Склад 3PL;  
\- дата: 31.07.2026;  
\- время: 14:06.

Приложенные протоколы считать контрольными примерами дефектов.

\#\# 1\. Клиент и проект

\`client\_name\` и \`project\_name\` должны:

1\. вводиться и редактироваться для любого элемента очереди;  
2\. сохраняться в BatchItem и batch state;  
3\. устанавливаться в Protocol до \`assemble\_from\_llm\_json()\`;  
4\. передаваться в LLM prompt;  
5\. после LLM повторно применяться как authoritative override;  
6\. сохраняться в protocol.json и pipeline\_result.json;  
7\. отображаться в обоих шаблонах;  
8\. попадать в Confluence title и Telegram.

В общей информации вывести: клиент, проект, тему, дату, время и источник.

Пример title: \`Протокол встречи от 31.07.2026. Роял Фуд — складские процессы и маркировка\`.

\#\# 2\. Участники и роли

Создать единую нормализованную модель участника с полями: name, role, organization, side, attendance\_status, evidence, confidence.

Поддержать aliases:

\- имя: name, full\_name, fio, имя, фио;  
\- роль: role, position, job\_title, title, должность, роль;  
\- организация: organization, company, side, сторона, организация.

Создать общий \`merge\_participants(metadata\_participants, llm\_participants, extracted\_participants)\`.

Правила:

1\. непустая подтвержденная роль не перезаписывается пустым значением;  
2\. LLM-участник без role не уничтожает роль из metadata;  
3\. оба шаблона используют один итоговый список;  
4\. должности не придумываются;  
5\. если роль неизвестна, выводить \`Не определено\`, а не прочерк;  
6\. сохранять \`participant\_merge\_report.json\`.

Добавить тест: metadata содержит роль, LLM возвращает только имя — роль должна сохраниться.

\#\# 3\. Настоящие списки

Создать \`normalize\_text\_list(value) \-\> list\[str\]\`.

Поддержать настоящий список, список объектов с полем text, JSON-строку массива, Markdown bullets, нумерацию \`1)\`, \`1.\`, символы \`•\`, \`-\` и переносы строк.

Удалять внешнюю нумерацию и пустые элементы. Разные пункты не объединять.

Для \`project\_detailed.key\_outcomes\` schema должна требовать массив из 3–12 строк. Prompt должен требовать каждый итог отдельным элементом без внутренней нумерации.

Строка \`1) ... 2\) ... 3\) ...\` должна стать минимум тремя отдельными \`\<li\>\`.

Для старых protocol.json обеспечить обратную совместимость.

\#\# 4\. Смысловые абзацы тематических блоков

Добавить в schema \`discussion\_paragraphs\` как массив строк.

Prompt должен требовать: один абзац — одна связная мысль или один аспект темы.

Создать \`normalize\_paragraphs(value) \-\> list\[str\]\`.

Если пришла одна строка:

1\. разбить на предложения;  
2\. объединить по 1–2 связанных предложения;  
3\. целевая длина абзаца 150–450 символов;  
4\. не менять факты и не добавлять новые.

Каждый абзац выводить отдельным \`\<p\>\`.

Контрольный блок про Barcode/марки должен визуально разделиться на: постановку вопроса; обычную маркируемую продукцию; агрегированные упаковки; получение данных через УПД/ЭДО и интеграционный контур.

\#\# 5\. Общий LLM extraction pass для реестров

Создать \`services/protocol\_register\_extractor.py\`.

Функция должна получать полный transcript, основной LLM JSON, тематические блоки, участников, клиента, проект и llm\_client и возвращать четыре массива: decisions, questions, risks, tasks.

Для \`project\_detailed\` и \`management\_summary\` extraction pass запускать всегда после основной генерации.

Каждый элемент хранит: текст, связанную тему, ответственного, срок, evidence, source timestamp и confidence.

\#\#\# Решения

Извлекать только подтвержденные итоги: решили, согласовали, договорились, выбрали, подтвердили, принято. Предложения не считать решениями.

\#\#\# Открытые вопросы

Извлекать не только предложения с вопросительным знаком, но и формулировки: требуется уточнить, необходимо проверить, вопрос требует проработки, пока не определено, нет подтверждения, нужно получить данные, окончательное решение не принято.

\#\#\# Риски и ограничения

Извлекать по смыслу: ограничения системы, зависимости, отсутствие данных, риск ошибки, риск потери истории, необходимость доработки и неподтвержденный технический сценарий. Не требовать буквального слова \`риск\`.

\#\#\# Задачи

Извлекать поручения и согласованные следующие действия: подготовить материалы, проверить сценарий, предоставить обратную связь, подготовить демонстрацию, провести следующую встречу. Ответственный и срок могут быть \`Не определено\`, если действие явно зафиксировано.

Для каждого элемента сохранять короткий подтверждающий фрагмент и таймкод, если он доступен.

\#\# 6\. Merge, дедупликация и retry

Создать \`merge\_protocol\_registers(main\_llm\_registers, extracted\_registers, atomic\_fallback)\`.

Приоритет: отдельный extraction pass → структурированный результат основной LLM → atomic fallback.

Пустой источник не стирает непустой. Выполнить дедупликацию.

Если найдены смысловые сигналы реестров, но все четыре массива пусты:

1\. выполнить один retry;  
2\. сохранить диагностику;  
3\. не писать ложное \`отсутствуют\`;  
4\. не публиковать результат как содержательно успешный;  
5\. сохранить локальные HTML и JSON.

Сохранять:

\- register\_extraction\_attempt\_1\_raw.txt;  
\- register\_extraction\_attempt\_1\_parsed.json;  
\- register\_extraction\_attempt\_2\_raw.txt;  
\- register\_extraction\_final.json;  
\- register\_merge\_report.json;  
\- participant\_merge\_report.json.

Не переносить extraction pass на следующую итерацию.

\#\# 7\. Реестры project\_detailed

Решения: №, решение, контекст/основание, ответственный, срок, связанная тема.

Открытые вопросы: №, вопрос, контекст, что определить, следующее действие, ответственный, срок, статус.

Риски: №, тип, риск/ограничение, причина, влияние, меры, ответственный, статус.

Задачи: №, задача, основание, ожидаемый результат, ответственный, срок, статус.

Пустой раздел можно скрыть только если extraction успешно завершен, сигналов нет и массив действительно пуст.

\#\# 8\. Исправить management\_summary

Целевая структура должна содержать:

\- general\_info: client\_name, project\_name, meeting\_date, meeting\_time, protocol\_title;  
\- participants;  
\- executive\_summary: overall\_status, summary, key\_changes;  
\- key\_results;  
\- decisions;  
\- risks;  
\- tasks;  
\- control\_points.

Каждое поле schema должно маппиться и отображаться.

Использовать общий participant merge и общий register extractor.

Контрольная точка — объект с полями control\_item, date, expected\_result, responsible, status и related\_task.

Запрещено сохранять пользовательские массивы через \`json.dumps()\` и выводить их как строковое представление списка.

Контрольная точка должна подтверждаться расшифровкой или задачей. Не создавать ее только из даты встречи.

Управленческое резюме объемом 150–400 слов должно отражать: текущий статус, изменения, согласования, незакрытые вопросы, критические риски и ближайшие действия.

\#\# 9\. Общие helpers renderer

Создать или доработать:

\- normalize\_text\_list;  
\- normalize\_paragraphs;  
\- render\_ordered\_list;  
\- render\_bullet\_list;  
\- render\_paragraphs;  
\- render\_optional\_table;  
\- render\_participants.

Экранировать значения LLM через \`html.escape\`, но не экранировать уже сформированные HTML tags повторно.

Не использовать \`str(list)\` или \`json.dumps(list)\` как пользовательский HTML.

\#\# 10\. Тесты

Создать:

\- tests/test\_participant\_merge.py;  
\- tests/test\_protocol\_list\_normalization.py;  
\- tests/test\_protocol\_paragraph\_rendering.py;  
\- tests/test\_protocol\_register\_extractor.py;  
\- tests/test\_project\_detailed\_registers.py;  
\- tests/test\_management\_summary\_content.py;  
\- tests/test\_client\_project\_rendering.py.

Обязательные проверки:

1\. metadata role не затирается пустой LLM-role;  
2\. alias position поддерживается;  
3\. оба шаблона используют один participant list;  
4\. client/project передаются в prompt и HTML;  
5\. numbered string становится несколькими \`\<li\>\`;  
6\. длинное discussion имеет несколько \`\<p\>\`;  
7\. extractor находит решения, вопросы без \`?\`, ограничения без слова \`риск\` и подтвержденные действия;  
8\. merge не стирает данные;  
9\. retry запускается при сигналах и пустых массивах;  
10\. ложное \`отсутствуют\` не выводится при ошибке extraction;  
11\. management summary непустой;  
12\. key results отображаются списком;  
13\. control points не отображаются JSON-строкой;  
14\. management summary использует общие реестры.

Запрещено использовать \`assert True\`.

\#\# 11\. Реальный E2E project\_detailed

Запустить \`call start\_app.bat\`.

Параметры: клиент Роял Фуд, проект Склад 3PL, шаблон project\_detailed.

Ожидается:

\- клиент и проект видны;  
\- клиент есть в Confluence title;  
\- дата 31.07.2026 и время 14:06;  
\- участники непусты, известные роли не потеряны;  
\- ключевых итогов не менее 5, каждый отдельный \`\<li\>\`;  
\- длинные темы разбиты на несколько \`\<p\>\`;  
\- решения, вопросы, риски и задачи непусты;  
\- каждый элемент имеет evidence;  
\- Confluence опубликован;  
\- Telegram содержит тот же URL.

\#\# 12\. Реальный E2E management\_summary

На той же расшифровке ожидается:

\- участники и роли заполнены;  
\- управленческое резюме и общий статус непусты;  
\- ключевые изменения и результаты представлены списками;  
\- решения, риски и задачи непусты;  
\- контрольные точки представлены таблицей или списком;  
\- в HTML нет строкового представления массива;  
\- Confluence опубликован;  
\- Telegram отправлен.

\#\# 13\. Проверки

\`\`\`  
python \-m compileall .  
pytest \-q  
pytest \-q tests/test\_participant\_merge.py  
pytest \-q tests/test\_protocol\_list\_normalization.py  
pytest \-q tests/test\_protocol\_paragraph\_rendering.py  
pytest \-q tests/test\_protocol\_register\_extractor.py  
pytest \-q tests/test\_project\_detailed\_registers.py  
pytest \-q tests/test\_management\_summary\_content.py  
pytest \-q tests/test\_client\_project\_rendering.py  
ruff check .  
mypy . \--ignore-missing-imports  
call start\_app.bat \--startup-check  
call start\_app.bat  
\`\`\`

Общий pytest без двух реальных E2E недостаточен.

\#\# 14\. Итоговая валидация

Создать:

\`docs/opencode/validations/VALIDATION-2026-08-03-\<NEW\_HEAD\_SHORT\>-PROTOCOL-CONTENT-REGISTERS-E2E.md\`

\#\# 15\. Ответ OpenCode

Предоставить:

1\. новый remote headRefOid, URL Draft PR и commit SHA;  
2\. список файлов и результаты команд;  
3\. пути к participant/register reports;  
4\. client/project;  
5\. число участников и участников с ролью;  
6\. число итогов и \`\<li\>\`;  
7\. максимальную длину тематического абзаца;  
8\. число решений, вопросов, рисков и задач;  
9\. по одному evidence каждого типа;  
10\. Confluence page ID/URL и Telegram message ID для обоих шаблонов;  
11\. подтверждение отсутствия строкового представления массивов в HTML;  
12\. путь к validation и оставшиеся ограничения.

Не писать \`готово\`, если реестры пусты. Не переносить extraction pass на следующую итерацию.

PR оставить Draft. Merge не выполнять. \`accepted\` не устанавливать.  
