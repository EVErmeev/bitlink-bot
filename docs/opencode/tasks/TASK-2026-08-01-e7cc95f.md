# TASK-2026-08-01-e7cc95f

**Создано:** 2026-08-01
**На основе:** AUDIT-2026-08-01-e7cc95f
**Базовый коммит:** `e7cc95fb0d375614849351cdef131f896dcec137`

---

## Порядок работы

1. **Создать ветку** `fix/audit-e7cc95f` от `main`.
2. **Обновить manifest.json:** `status = in_progress`, `validation_state = implementing_fixes`.
3. **Сначала исправить все BB-CRIT-001 … BB-CRIT-010.**
4. **Затем исправить BB-MAJ-011 … BB-MAJ-020.**
5. **Не выдавать mock за реальную интеграцию.** Реализовать HTTP-адаптеры. Mock включается только явно через `*_MOCK=true`. При `*_MOCK=false` и отсутствии конфигурации — ошибка.
6. **Реализовать отдельный LLM-слой** — конфигурируемый провайдер, вызов по промпту шаблона.
7. **Реализовать четыре независимых assembler** — каждый шаблон формирует свой контент.
8. **Исправить обработку ошибок валидации** — разделить `completed` и `validation_failed`.
9. **Повторять все проверки после `apply_corrections()`** — факты, структура, alignment, HTML.
10. **Использовать SHA-256 содержимого** как первичный seed для `source_context_id`.
11. **Реализовать dry-run** — полный конвейер кроме публикации в Confluence и Telegram.
12. **Реализовать восстановление очереди** — авто-загрузка `batch_state.json`, диалог в GUI.
13. **Реализовать повторную публикацию** — чтение готового JSON/HTML без вызова LLM.
14. **Добавить end-to-end и golden tests** — реальные транскрипты, ожидаемые протоколы.
15. **Добавить GitHub Actions** — workflow для Windows и Linux:
    ```yaml
    steps:
      - run: python -m compileall .
      - run: pip install -r requirements.txt
      - run: pytest -q
      - run: ruff check .
      - run: mypy .
    ```
16. **Запустить проверки локально:**
    ```bash
    python -m compileall .
    pytest -q
    ruff check .
    mypy .
    ```
17. **Обновить manifest.json:**
    - `head_commit` — новый SHA
    - `pull_request_url` — URL PR
    - `status = awaiting_independent_validation`
    - `validation_state = awaiting_review`
18. **Выполнить push:**
    ```bash
    git add -A
    git commit -m "fix: address AUDIT-2026-08-01-e7cc95f (BB-CRIT-* + BB-MAJ-*)"
    git push -u origin fix/audit-e7cc95f
    ```
19. **Создать Pull Request** в `main`.
20. **Остановиться.** Статус: `awaiting_independent_validation`.

---

## Замечания к исправлению

### BB-CRIT-001 … BB-CRIT-010

| ID | Суть | Приоритет |
|----|------|-----------|
| BB-CRIT-001 | Mock → реальные HTTP-адаптеры | Первый |
| BB-CRIT-002 | Отсутствует LLM-слой | Первый |
| BB-CRIT-003 | Все факты в одну тему | Высокий |
| BB-CRIT-004 | Три шаблона пустые | Высокий |
| BB-CRIT-005 | Провал валидации = completed | Высокий |
| BB-CRIT-006 | Нет повторной проверки после коррекций | Высокий |
| BB-CRIT-007 | source_context_id от пути, не от SHA | Средний |
| BB-CRIT-008 | Dry-run не реализован | Средний |
| BB-CRIT-009 | Восстановление очереди не реализовано | Средний |
| BB-CRIT-010 | Повторная публикация — заглушка | Средний |

### BB-MAJ-011 … BB-MAJ-020

| ID | Суть | Приоритет |
|----|------|-----------|
| BB-MAJ-011 | ETA без сохранения истории | Низкий |
| BB-MAJ-012 | Дата БИТ.Link не используется | Средний |
| BB-MAJ-013 | Настройки шаблона игнорируются | Средний |
| BB-MAJ-014 | Нет экранирования HTML | Высокий |
| BB-MAJ-015 | Тесты только mock | Средний |
| BB-MAJ-016 | Watch — заглушка | Низкий |
| BB-MAJ-017 | Нет batch report | Средний |
| BB-MAJ-018 | Telegram не читает settings | Средний |
| BB-MAJ-019 | Confluence — только mock | Средний |
| BB-MAJ-020 | Остановка очереди неполная | Средний |

---

## Критерии готовности

- [ ] Все BB-CRIT-* исправлены
- [ ] Все BB-MAJ-* исправлены
- [ ] `python -m compileall .` — без ошибок
- [ ] `pytest -q` — все тесты проходят
- [ ] `ruff check .` — без ошибок
- [ ] `mypy .` — без ошибок
- [ ] Добавлены golden tests
- [ ] GitHub Actions проходит
- [ ] PR создан, статус `awaiting_independent_validation`
