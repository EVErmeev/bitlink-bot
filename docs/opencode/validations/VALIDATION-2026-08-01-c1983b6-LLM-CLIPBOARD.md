\# VALIDATION-2026-08-01-c1983b6-LLM-CLIPBOARD

\#\# Объект проверки

\- Repository: \`EVErmeev/bitlink-bot\`  
\- Draft PR: \`https://github.com/EVErmeev/bitlink-bot/pull/1\`  
\- Фактический head PR: \`c1983b6fbfa3ed65c0f13982c73e832d14dfcea7\`  
\- Сообщённый OpenCode SHA: \`c1983b60e0c1a9779b1b947cb7126310f7f8da20\`

\#\# Итоговый статус

\`CHANGES\_REQUIRED\`

PR оставить Draft. Merge запрещён.

\#\# Подтверждённое исправление

Регрессия вызова \`run\_preflight(..., source\_types=...)\` устранена: GUI снова доходит до preflight.

\#\# Фактическое пользовательское поведение

При запуске local TXT отображается:

\`\`\`text  
LLM в mock-режиме  
\`\`\`

Это ожидаемо и означает, что рабочая LLM-генерация не настроена. Результат должен оставаться demo dry-run.

\#\# Блокирующие замечания

\#\#\# BB-CRIT-056 — real LLM отсутствует в Settings GUI

В интерфейсе нет LLM mode, Base URL, API Key, Model и structured JSON connection test. Пользователь не может перевести приложение из mock в real mode через GUI.

\#\#\# BB-CRIT-057 — стандартный буфер обмена не работает

Пользователь не может использовать Ctrl+C и Ctrl+V в формах приложения. Глобальные keyboard bindings в \`app.py\` и \`MainWindow\` отсутствуют.

Проблема особенно критична для длинных значений:

\- API Key;  
\- Telegram token;  
\- Confluence token;  
\- Base URL;  
\- Chat ID;  
\- Parent Page ID.

\#\#\# BB-MAJ-058 — mock warning не ведёт к настройке LLM

Предупреждение сообщает о mock-режиме, но не даёт действия \`Открыть настройки LLM\`.

\#\#\# BB-MAJ-059 — LLMClient не поддерживает проверку несохранённого режима

Конструктор получает URL/Key/Model, но \`mock\_mode\` всегда берётся из глобального \`settings.LLM\_MOCK\`. Поэтому form connection test не может надёжно проверить выбранный пользователем unsaved режим \`Real API\`.

\#\#\# BB-MAJ-060 — неверный head SHA в отчёте OpenCode

Сообщённый полный SHA не существует в PR. Фактический head отличается после короткого префикса.

\#\# Решение по mock warning

Предупреждение не удалять. До настройки real LLM оно должно явно сообщать:

\`\`\`text  
Демонстрационный режим.  
Протокол формируется тестовым mock-генератором.  
Рабочая публикация запрещена.  
\`\`\`

Должны быть доступны действия:

\- продолжить demo dry-run;  
\- открыть настройки LLM;  
\- отменить.

\#\# Условия повторной проверки

1\. LLM полностью настраивается через GUI.  
2\. Проверка использует несохранённые значения формы.  
3\. После сохранения RuntimeConfig и banner обновляются без перезапуска.  
4\. Ctrl+C/Ctrl+V/Ctrl+X/Ctrl+A работают во всех текстовых формах.  
5\. Горячие клавиши работают в английской и русской раскладке Windows.  
6\. Доступно контекстное меню правой кнопки мыши.  
7\. Реальный API Key не попадает в репозиторий и validation artifacts.  
